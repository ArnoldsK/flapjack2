import type { AppContext } from "@app/context";
import type { Video } from "@project-types/video";

/**
 * List videos. Stub implementation; wire to DB or external API later.
 */
export const list = async (_ctx: AppContext): Promise<Video[]> => [];
