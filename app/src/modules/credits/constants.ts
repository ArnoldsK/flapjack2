export const UPPER_CLASS_CREDITS = 500_000n;

/** Cost in credits per message in the upper-class channel. */
export const UPPER_CLASS_MESSAGE_CREDITS = 500_000n;

/** Casino activity within this window counts as active on the credits top leaderboard. */
export const TOP_ACTIVE_WITHIN_DAYS = 7;
